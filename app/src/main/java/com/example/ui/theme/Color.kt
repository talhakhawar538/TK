package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Primary colors - Replaced with Tailwind Blue-600 & Deep Royal gradients for "Professional Polish"
val EmeraldPrimary = Color(0xFF2563EB)      // Vibrant Pro Blue
val EmeraldSecondary = Color(0xFF1E3A8A)    // Deep Navy
val EmeraldTertiary = Color(0xFF3B82F6)     // Accent Sky Blue

// Re-mapped names matching the existing screen layouts, but transformed to elegant Slate, White & Blue
val StadiumGrass = Color(0xFF2563EB)        // Corporate Blue-600 (Action Primary)
val GoldAccent = Color(0xFFDF8F00)          // Premium Amber gold
val DarkBackground = Color(0xFFF7F9FC)      // Sleek slate-white background
val CardSurfaceDark = Color(0xFFFFFFFF)     // Clean white cards
val CardSurfaceLight = Color(0xFFEFF6FF)    // Tinted highlight blue list backgrounds
val OutlinedContainerBorder = Color(0xFFE2E8F0) // Soft light slate border

val PureWhite = Color(0xFF0F172A)           // Inverse styling: PureWhite becomes Slate-900 dark text!
val RealWhite = Color(0xFFFFFFFF)           // True white for badges or icons drawn over dark/solid shapes
val MutedText = Color(0xFF64748B)           // Slate-500 description text
val ScoreOrange = Color(0xFFEA580C)         // Crisp primary orange
val WicketRed = Color(0xFFEF4444)           // Modern soft warning red
